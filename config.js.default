'use strict';

let config = {


};


function getConfigByKey(key = null)
{
	let result = config;
	if(key && config.hasOwnProperty(key)){
		result = config[key];
	}
	return result;
}

module.exports.getConfigByKey = getConfigByKey;